name: "CI Nix"
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  nix-ci:
    if: ${{ github.head_ref != 'mitm-pilot' }} # MITM replay CI dry-run: skip unrelated checks on this branch
    runs-on: ${{ matrix.system }}
    permissions:
      contents: read
    strategy:
      matrix:
        system: [x86_64-linux, aarch64-darwin]
    steps:
      - uses: actions/checkout@v6
      - run: om ci run --extra-access-tokens ${{ secrets.GITHUB_TOKEN }} --systems "${{ matrix.system }}"
