# Mac OS files
.DS_Store

# Output files
reports/
screenshots/
videos/

creds.json
run_all.sh

# Cypress Dashboard build
**/dist

# Local redirect-proxy body recordings (not committed — stored in CAPTURE_DIR in CI)
cypress/fixtures/proxy-bodies/